# macOS
.DS_Store

# Python bytecode (py_compile, hook runs)
__pycache__/
*.pyc

# Harness verification stamp (single-use, never committed)
.harness-verified

# Internal planning + handoff docs (local-only, not distributed)
docs/plans/
docs/handoff/
